#include<stdio.h>
int main()
{
   int val,key;
   
   printf("enter two no\t");
   scanf("%d%d",&val,&key);
   
   
val=val/key;

   printf("after setting bit register is %d/t",val);
    return 0;
}